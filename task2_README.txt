ISSUE: Discount strategies are not models. That means that they have no id's. So I used Class object as id inside statistic model.
Not sure if it works for DB persistance. Maybe "StrategyTypes" table should be added and I'll use id's from that table.
